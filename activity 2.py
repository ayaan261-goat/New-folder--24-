import pygame

pygame.init()
SCREEN_WIDTH, SCREEN_HEIGHT = 500, 500

display_surface = pygame.display.set_mode((SCREEN WIDTH , SCREEN_HEIGHT))
pygame.display.set_caption('adding image and backround image')
background_
                           